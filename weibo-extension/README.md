# Weibo Hot Search Filter

一个可直接加载到浏览器的 `Manifest V3` 插件，用于按自定义文本屏蔽微博热搜，并把命中项直接从列表移除。

## 功能

- 自定义添加多条屏蔽文本
- 文本匹配忽略大小写、空格和常见符号
- 热搜列表动态变化时自动重扫并移除命中项

## 使用方式

1. 打开 Chrome 或 Edge 的扩展管理页。
2. 开启“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择目录：`weibo-extension`
5. 打开微博热搜页：`https://s.weibo.com/top/summary`
6. 点击插件图标，添加你要屏蔽的文本。

## 匹配说明

- 会先尝试在原始文本中直接包含匹配
- 同时使用规范化文本匹配，增强对空格和符号变化的兼容
